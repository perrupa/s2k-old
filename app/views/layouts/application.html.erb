<!DOCTYPE html>
<html xmlns:og="http://ogp.me/ns#" xmlns:fb="http://www.facebook.com/2008/fbml">
<head>
	<title>Skills to Kill</title>

	<%= csrf_meta_tag %>
	
	<%= stylesheet_link_tag :all %>
	<%= javascript_include_tag :defaults %>

	<%= yield(:head) %>
	<meta property="og:site_name" content="Skills to Kill"/>
	<meta property="og:url" content="<%= request.url %>"/>
	<meta property="fb:admins" content="601315226"/>
</head>
<body>
	<div class="topbar">
		<%= render :partial => 'layouts/topbar' %>
	</div>

	<div class="container">
		<div class="content">
			<%= yield %>
		</div>
	</div>

	<footer>
		<p>&copy;<%= Time.now.year %> Chris Marlow</p>
	</footer>

</body>
</html>
